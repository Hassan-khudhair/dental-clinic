{
  "version": 2,
  "installCommand": "pnpm install && npx prisma generate",
  "buildCommand": "nest build",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
